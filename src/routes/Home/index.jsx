import React from 'react';
import './index.css';
import Layout from "@/Hoc/Layout";

const Page = () => (
    <React.Fragment>

    </React.Fragment>
);

export default Layout({ name: 'home' })(Page);
